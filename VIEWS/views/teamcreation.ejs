<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Create or Select Team</title>
    <link rel="stylesheet" href="styles.css"> <!-- Link your CSS -->
</head>
<body>
    <div class="container">
        <h1>Create or Select Team</h1>

        <!-- Form to Create a New Team -->
        <form id="createTeamForm" method="POST" action="/createTeam">
            <h2>Create New Team</h2>
            <label for="teamName">Team Name:</label>
            <input type="text" id="teamName" name="teamName" required>
            
            <label for="teamDescription">Team Description:</label>
            <textarea id="teamDescription" name="teamDescription" required></textarea>

            <button type="submit">Create Team</button>
        </form>

        <hr>

        <!-- List of Existing Teams -->
        <h2>Existing Teams</h2>
        <ul id="teamList">
            <!-- This should be populated dynamically with server-side code -->
            <% teams.forEach(team => { %>
                <li>
                    <span><strong><%= team.name %></strong> - <%= team.description %></span>
                    <form action="/chooseDomain" method="POST" style="display:inline;">
                        <input type="hidden" name="teamId" value="<%= team.id %>">
                        <button type="submit">Select Team</button>
                    </form>
                </li>
            <% }); %>
        </ul>
    </div>
</body>
</html>
